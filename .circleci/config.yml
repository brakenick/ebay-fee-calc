
version: 2

jobs:
  build:
      working_directory: ~/
      
test:
  bundle exec htmlproofer ./ --check-html --disable-external

deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync ./ s3://ebay-fee-calc/
